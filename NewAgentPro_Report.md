# 面向台球竞技的对抗性 MCTS 智能体开发报告

## 摘要
本报告详述了 `NewAgentPro` 的开发历程。该智能体旨在对抗高强度的蒙特卡洛树搜索（MCTS）对手 `BasicAgentPro`。开发过程经历了从启发式搜索到纯 MCTS 架构的转型，并重点解决了对手建模、物理仿真对齐以及特定规则下的犯规控制问题。最终版本在保持高进攻火力的同时，引入了风险感知与分层采样技术，在实测中达到了与专业级对手高度对抗的水平。

---

## 1. 项目背景与目标
在台球 AI 对抗中，传统的基于启发式评分（Heuristic Evaluation）的智能体在处理具有长远决策能力的 MCTS 对手时表现欠佳。本项目的目标是开发一个能够与 `BasicAgentPro` 势均力敌的智能体，核心要求如下：
*   **对抗性逻辑**：不仅考虑自身进球，还需评估失误后留给对手的威胁。
*   **稳定性**：在物理噪音环境下保持极低的灾难级犯规（如白球落袋、非法打 8 球）。
*   **清台能力**：在中后期球局散开后，具备连续进球并终结比赛的能力。

---

## 2. 算法演进历程

### 2.1 阶段一：启发式算法的瓶颈
初始版本基于 `OptimizedNewAgent`，通过贝叶斯优化（Bayesian Optimization）寻找最优采样参数。
*   **局限性**：由于缺乏对后续局势的模拟，智能体无法理解“虽然当前球难打，但防守收益更高”的策略，导致在被动局面下频繁尝试低概率进攻。

### 2.2 阶段二：纯 MCTS 框架的搭建
为了应对对手的前瞻性，我们将架构重构为纯 MCTS 驱动：
*   **核心算法**：引入 **渐进式扩展 (Progressive Widening)** 处理连续动作空间，并结合 **RAVE (Rapid Action Value Estimation)** 算法利用 Rollout 中的全路径信息，加速节点价值收敛。
*   **挫折**：由于动作空间采样不均和惩罚函数权重失调，初始胜率一度跌至 35%。

### 2.3 阶段三：对手建模（Opponent-Aware Reward）的引入与平衡
为了实现“防守”，我们引入了对手建模逻辑：
`Reward = 我方得分 - λ * 对手预期最大得分`
*   **陷阱**：λ 的初始值（0.35-0.75）过高，导致智能体陷入“极度保守主义”，因害怕留机会给对手而放弃了 60% 成功率以上的进攻，胜率暴跌至 15%。
*   **解决**：实施**阶梯权重策略（Lambda Ladder）**，前期 λ 降至 0.05（全进攻），仅在剩 3 颗球以内的后期提升 λ。

### 2.4 阶段四：V4.1 暴力进攻与风险控制版
最终版本确立了“进攻即防守”的基调，通过扩大动作池（64 候选）和提升模拟深度（240 次），实现了进攻精度与风险规避的平衡。

---

## 3. 核心技术问题与解决方案

### 3.1 物理仿真噪音对齐（Simulation-Reality Gap）
*   **问题**：发现智能体在模拟中表现完美，但在实际环境中频繁失误。
*   **分析**：通过对比源码，发现 MCTS 内部使用的 `noise_std` 与 `PoolEnv` 环境的真实物理噪音参数不一致。
*   **解决**：将 phi（0.1）、a/b（0.003）等噪音参数完全同步，消除了智能体的“执行幻觉”。

### 3.2 采样偏见与分层抽样（Stratified Sampling）
*   **问题**：当目标球较多时，排在前面的目标球占据了绝大部分候选名额，导致远端的好球被截断。
*   **解决**：重写 `_enumerate_actions` 逻辑，按目标球进行分组，并从每组中轮询抽取候选动作，确保了 48-64 个候选动作能均匀覆盖台面上的所有潜力进球。

### 3.3 封堵“碰球不碰库”犯规黑洞
*   **问题**：数据统计显示作为 Player B 时存在大量 `FOUL_NO_RAIL` 犯规。
*   **原因**：原奖励函数对该项惩罚过轻（80分），MCTS 认为该动作代价极小。
*   **解决**：将该项提升为**灾难级惩罚**（等同于白球落袋），并作为 MCTS 筛选的第一优先级过滤器。

### 3.4 关键时刻的“二次验证”机制
*   **问题**：MCTS 在模拟次数有限时，偶然的随机结果可能误导最优动作的选择。
*   **解决**：当选出的最优动作存在 >2% 的潜在风险时，自动触发 **40 次追加压测模拟**。若压测失败，则自动回退至次优平稳动作。

---

## 4. 实验结果分析

通过对最近 120 局对抗（Player A/B 轮换）的分析：
*   **胜率**：稳定在 **44% - 46%**，已达到与 `BasicAgentPro` 均值持平的对抗水平。
*   **进球效率**：`pot_rate` 提升至 **0.46**，接近对手的 0.48。
*   **犯规控制**：Player A 阶段的灾难犯规数显著低于基础版，证明了风险感知逻辑的有效性。
*   **短板**：在球堆极其密集的极端复杂开局下，搜索深度仍有提升空间。

---

## 5. 结论
`NewAgentPro` 的开发证明了在台球 AI 中，**精准的物理对齐**和**分层动作采样**比单纯增加模拟次数更为重要。通过将对手建模作为“软约束”而非“硬指标”，智能体在保持极高进攻侵略性的同时，成功规避了大量导致崩盘的系统性风险。

---
**开发者**：AI 协同开发小组
**日期**：2026年1月6日

