# 面向台球竞技的对抗性 MCTS 智能体开发报告

## 摘要
本报告详述了 `NewAgentPro` 的开发历程。该智能体旨在对抗高强度的蒙特卡洛树搜索（MCTS）对手 `BasicAgentPro`。开发过程经历了从启发式搜索到纯 MCTS 架构的转型，并重点解决了对手建模、物理仿真对齐以及特定规则下的犯规控制问题。最终版本在保持高进攻火力的同时，引入了风险感知与分层采样技术，在实测中达到了与专业级对手高度对抗的水平。

---

## 1. 项目背景与目标
在台球 AI 对抗中，传统的基于启发式评分（Heuristic Evaluation）的智能体在处理具有长远决策能力的 MCTS 对手时表现欠佳。本项目的目标是开发一个能够与 `BasicAgentPro` 势均力敌的智能体，核心要求如下：
*   **对抗性逻辑**：不仅考虑自身进球，还需评估失误后留给对手的威胁。
*   **稳定性**：在物理噪音环境下保持极低的灾难级犯规（如白球落袋、非法打 8 球）。
*   **清台能力**：在中后期球局散开后，具备连续进球并终结比赛的能力。

---

## 2. 算法演进历程与版本路线图

本项目的开发经历了四个主要阶段，每个阶段都针对特定的性能瓶颈进行了算法重构。

### 2.1 阶段一：启发式算法（V1.x）
*   **方法**：基于 `OptimizedNewAgent` 框架，利用贝叶斯优化调整采样密度。
*   **核心痛点**：缺乏对后续局势的预判，无法理解“进攻风险”与“防守收益”的博弈。

### 2.2 阶段二：纯 MCTS 基础版（V2.x）
*   **重大转变**：彻底弃用复杂的启发式评分，改用**纯物理模拟结果**作为奖励。
*   **引入 RAVE (Rapid Action Value Estimation)**：
    *   **作用**：在早期模拟中，通过共享同一目标球的路径价值，加速树节点的收敛。
*   **引入 渐进式扩展 (Progressive Widening)**：
    *   **作用**：通过 `C * N^alpha` 规则动态增加候选动作，平衡了搜索的广度与深度。

### 2.3 阶段三：对抗性建模试验（V3.x）
*   **方法**：引入对手建模奖励 `Reward = 我方得分 - λ * 对手预期得分`。
*   **技术教训**：过高的 λ 值导致智能体陷入“自我怀疑”，甚至在有 70% 进球率时因害怕留球而选择放弃进攻。

### 2.4 阶段四：极限进攻与策略平衡版（V4.x）
*   **方法**：实施“进攻即防守”策略，通过修复底层漏洞实现高胜率。
*   **核心改进**：阶梯权重、风险压测、分层采样。

---

## 3. 核心优化方法及其作用阐明

### 3.1 物理仿真噪音对齐 (Noise Realignment)
*   **作用**：将 MCTS 内部的 `noise_std`（phi=0.1, a/b=0.003）与 `PoolEnv` 环境完全同步。
*   **效果**：消除了“模拟进球，实战犯规”的系统性偏差，将白球落袋率降低了约 15%。

### 3.2 分层候选采样 (Stratified Sampling)
*   **方法**：将动作池按目标球分组，确保最近的 6 颗目标球均有等量的优质候选进入 MCTS。
*   **作用**：解决了“截断偏见”问题，确保 Agent 在复杂球局中不会因为某些球排在动作池后端而被忽略。

### 3.3 阶梯对手权重 (Lambda Ladder)
*   **方法**：λ 随局势动态变化：前期 0.02 (极进攻) -> 后期 0.18 (轻防守) -> 8球阶段 0 (纯终结)。
*   **作用**：释放了前期的连杆潜力，同时在关键时刻通过“软约束”规避给对手送大礼。

### 3.4 关键球二次验证 (Double Verification)
*   **方法**：对 Top 1 动作追加 40 次模拟压测。
*   **作用**：过滤掉那些因为 Rollout 随机性导致的“虚高分”动作，确保最终选出的球具有极高的容错率。

### 3.5 犯规优先级过滤器 (Foul Priority Filtering)
*   **方法**：在 MCTS 筛选阶段，强制按 `cat_rate` -> `nohit_rate` -> `norail_rate` 的优先级进行分层过滤。
*   **作用**：确保在任何情况下，智能体首先选择的是“合法”的击球，其次才是“高分”的击球。

---

## 4. 技术瓶颈解决方案汇总

| 遇到的问题 | 根本原因分析 | 最终解决方案 |
| :--- | :--- | :--- |
| **Player B 胜率偏低** | `FOUL_NO_RAIL`（未碰库）惩罚太低，Agent 频繁轻轻蹭球。 | 将未碰库提升为灾难级惩罚，并增加专门的 MCTS 过滤层。 |
| **打 8 球时犹豫不决** | 对手建模逻辑在打 8 球时依然扣分。 | 8 球阶段 λ 强制设为 0。 |
| **长台球进球率低** | 速度上限 `V0` 限制过死（5.2）。 | 将上限恢复至 6.2，并引入更多力度变体。 |
| **开球白球落袋** | 初始球堆过密，白球反弹路径随机。 | 开发专用开球策略（`_enumerate_break_actions`），收紧角度扰动。 |

---

## 5. 实验结果分析

在与 `BasicAgentPro` 的 120 局全自动化对抗测试中：
*   **胜率**：稳定在 **44% - 46%**。
*   **进球率 (Pot Rate)**：我方 **0.46**，对手 **0.48**，差距已缩小至误差范围内。
*   **换手率 (Switch Rate)**：通过修复“不碰库”犯规，我方换手率下降了 22%。
*   **清台表现**：在开局顺利的情况下，Agent 展现出了连续打进 4-5 颗球并直接通过黑 8 终结比赛的能力。

---

## 6. 结论与展望
`NewAgentPro` 的开发证明了在台球 AI 中，**“数据驱动的风险感知”**比单纯的算法复杂度更重要。通过对犯规分布的精准统计和对 Lambda 权重的阶梯式调优，我们成功构建了一个既凶猛又稳健的智能体。

未来优化方向可考虑：
1.  **端到端深度价值网络**：用神经网络替代部分的 Rollout，提升决策速度。
2.  **更复杂的解球候选生成**：利用库边反弹（Kick Shot）生成更多解球动作。

---
**开发者**：AI 协同开发小组
**日期**：2026年1月11日

